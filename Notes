node -v 
npm -v 
ng -v 
npm install -g @angular/cli
ng analytics off
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
ng new hello-world
ng g c anyname // component creation
ng g s anyname  // service creation 
cd hello-world
npm install
ng serve
http://localhost:4200/
ng g c test
(g --> generate  c --> component  )
-------------------------------
Boot strap and CSS and angular.json file

ng install bootstrap

"styles": [
              "node_modules/bootstrap/dist/css/bootstrap.min.css",
              "src/styles.css"
          ]

We can change the port number and hostname dynamically 
------------------------------------------
ng serve --host 0.0.0.0 --port 4201

Component	ng g component my-new-component
Directive	ng g directive my-new-directive
Pipe	    ng g pipe my-new-pipe
Service  	ng g service my-new-service
Class	    ng g class my-new-class
Guard		ng g guard my-new-guard
Interface	ng g interface my-new-interface
Enum		ng g enum my-new-enum
Module		ng g module my-module

angular-cli will add reference to components, directives and pipes automatically in the app.module.ts. 
If you need to add this references to another custom module, follow these steps:

* ng g module new-module to create a new module
* ng g component new-module/new-component

Updating Angular CLI
If you're using Angular CLI 1.0.0-beta.28 or less, you need to uninstall angular-cli package. It should be done due to changing of package's name and scope from angular-cli to @angular/cli:

npm uninstall -g angular-cli
npm uninstall --save-dev angular-cli

Global package:
-----------------
npm uninstall -g @angular/cli
npm cache verify
# if npm version is < 5 then use `npm cache clean` 
npm install -g @angular/cli@latest

npm cache clean --force



https://docs.npmjs.com/cli/link  

CPU Utilization 
---------------
https://github.com/angular/angular-cli/issues/8259

Cli commands 
-------------
https://github.com/angular/angular-cli



Local project package:
----------------------
rm -rf node_modules dist # use rmdir /S/Q node_modules dist in Windows Command Prompt; use rm -r -fo node_modules,dist in Windows PowerShell 
npm install --save-dev @angular/cli@latest
npm install

https://code.visualstudio.com/shortcuts/keyboard-shortcuts-windows.pdf

ng update
ng add @angular/material
ng generate @angular/material:material-nav --name=my-nav
ng generate @angular/material:material-dashboard --name=my-dashboard
ng generate @angular/material:material-table --name=my-table

Supports multipel projects
--------------------------

angular.json instead of angular-cli.json

https://github.com/gopinav

https://update.angular.io/



Angular5 to Angular6
--------------------
HttpModule -> HttpClientModule
              HttpClient
----------------------------
At leaset node version should be more than 8 > 

node -v  in command promt

new Angular.json format
---------------------
npm install -g @angular/cli
npm install @angular/cli
npm update @angular/cli

Update all angular framework packages to v6 and correct version of Rxjs and typescript
------------------------------------------------------------  
ng update @angular/core

Update angular material 
-----------------------
np update @angular/material 












